{
  "name": "cypress-cucumber-tests",
  "version": "1.0.0",
  "description": "Cypress tests with Cucumber support",
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "test": "cypress run --spec 'cypress/integration/**/*.feature'"
  },
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^23.2.1",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.0",
    "@types/node": "^24.9.2",
    "cypress": "^15.5.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
